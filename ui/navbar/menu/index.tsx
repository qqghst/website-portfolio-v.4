import React from 'react';
import styles from './styles.module.scss';

const Menu = () => {
    return (
        <div>
            <span className={styles.span}>Menu</span>
        </div>
    );
};

export default Menu;
